<%- include('./partials/_head',{title:"List"}) %>
  <body>
    <%- include('./partials/_header') %> 
    <main>
    <section class="org_webURL_list">
        
        <ul>

            <% if(!locals.webURL_arr || locals.webURL_arr.length === 0){%>
              <p>Admin hasn't created any.</p>
            <% }else{ %>  
            <% for(const key of webURL_arr) { %> 
              <%- include('./partials/_webinfo_displayOnly_card',{webURL_item:key})  %> 
            <% } }%> 

        </ul>
        
    </section>
    </main>
    <%- include('./partials/_footer') %> 
  </body>
</html>
